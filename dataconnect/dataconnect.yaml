specVersion: "v1"
serviceId: "repo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "repo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
